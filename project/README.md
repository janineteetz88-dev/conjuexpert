# ⚠️ NICHT LIVE

Dieser Ordner (`/project/`) wird **nicht** ausgeliefert und ist **nicht** die
veröffentlichte App.

- **Live ist:** das gebündelte `/index.html` im Repo-Wurzelverzeichnis
  (= `https://conjuexpert.app`). Es enthält seinen gesamten Code selbst und
  lädt **kein** `app.jsx`.
- **Dieser Ordner** ist ein älterer/separater Arbeitsstand. Nichts im Repo
  verlinkt darauf, und es ist nicht in `sitemap.xml` eingetragen.
- Ihm fehlen Funktionen, die nur im Live-Stand existieren (z. B. die
  männlich/weiblich-Stimmenauswahl).

**Änderungen in diesem Ordner wirken sich NICHT auf die Webseite aus.**
Für Änderungen an der Live-App das Wurzel-`/index.html` bearbeiten.
